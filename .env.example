# MySQL Configuration (for Docker Compose)
# These values are used to initialize the MySQL container
# And are also passed to the frontend application as DB credentials
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_USER=lazykanban
MYSQL_PASSWORD=your_secure_password_here

# Security
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# Deployment / Production
# Host port for the Traefik proxy
CLIENT_PORT=3000
# Domain for Traefik routing
DOMAIN=localhost
# The full public URL of the application (used for CORS and socket.io)
NEXT_PUBLIC_APP_URL=http://localhost:3000
